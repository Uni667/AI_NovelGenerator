# JWT signing secret. Required in production. Generate a long random value.
NEXTAUTH_SECRET=

# API key encryption secret for stored provider credentials. Required in production.
API_SECRET_ENCRYPTION_KEY=

# Comma-separated frontend origins allowed by the backend CORS middleware.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
