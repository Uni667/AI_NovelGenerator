# JWT 签名密钥（必填，支持 NEXTAUTH_SECRET 或 JWT_SECRET）
NEXTAUTH_SECRET=
# API Key 加密密钥（必填，用于 AES-256-GCM 加密存储 API Key）
API_SECRET_ENCRYPTION_KEY=
# CORS 允许的前端地址（逗号分隔）
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
